import React from "react";
export default function Menu() {
  // set #IDs to link to on the page
  return (
    <header>
      <ul>
        <li id="about">About</li>
        <li id="work">Work</li>
      </ul>
    </header>
  );
}
